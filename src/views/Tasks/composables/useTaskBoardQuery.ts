import { ref, computed, watch, type Ref } from 'vue';
import { useRoute, useRouter } from 'vue-router';
import { useI18n } from 'vue-i18n';
import { useAuthStore } from '@/stores/auth';
import { getTaskDayIndex, getTaskTime } from '@/utils/taskSort';
import { isTaskOverdue } from '@/utils/taskDisplay';
import { TaskStatus } from '@/types/task';
import type { Task, UserType, Project } from '@/types/task';
type ViewMode = 'board' | 'list' | 'calendar';
type SortBy = 'natural' | 'createdAt_asc' | 'createdAt_desc';
type GroupBy = 'status' | 'priority' | 'assignee' | 'dueDate';
export interface TaskBoardColumn {
  id: string;
  title: string;
  color: string;
  headerBg: string;
}
const loadViewMode = (): ViewMode => {
  const saved = localStorage.getItem('task_view_mode');
  return saved === 'list' || saved === 'board' || saved === 'calendar' ? saved : 'list';
}; /** * Route / query / filter / sort / grouping state for the TaskBoard, kept * separate from the view container so the route + query layer can evolve (and * be tested) independently. * * Receives the shared `tasks` / `teamMembers` / `projects` refs (owned by the * root `useTaskBoard` composable) and derives every filtered / grouped / * view-ready list consumed by the Board / List / Calendar sub-views. */
export function useTaskBoardQuery(
  tasks: Ref<Task[]>,
  teamMembers: Ref<UserType[]>,
  projects: Ref<Project[]>,
) {
  const { t } = useI18n();
  const route = useRoute();
  const router = useRouter();
  const authStore = useAuthStore(); // --- query / filter state --- const searchQuery = ref<string>(''); const dateFilter = ref<string>('all'); const statusFilter = ref<string>('all'); const priorityFilter = ref<string>('all'); const customDate = ref<string>(''); const hideCompleted = ref<boolean>(false); const onlyMyTasks = ref<boolean>(false); const sortBy = ref<SortBy>((localStorage.getItem('task_sort_by') as SortBy | null) || 'natural'); const sortOrder = ref<'asc' | 'desc'>('asc'); const assigneeFilter = ref<string>('all'); const tagFilter = ref<string>('all'); const subtaskDisplay = ref<'collapse' | 'expand' | 'separate'>('collapse'); watch(sortBy, (newVal) => { localStorage.setItem('task_sort_by', newVal); }); // --- project filter (synced with URL ?projectId) --- const selectedProjectId = ref<string | null>((route.query.projectId as string) || null); watch( () => route.query.projectId, (newVal) => { selectedProjectId.value = (newVal as string) || null; }, ); watch(selectedProjectId, (newVal) => { const currentQuery = { ...route.query }; if (newVal) { if (currentQuery.projectId !== newVal) { router.replace({ query: { ...currentQuery, projectId: newVal } }); } } else { if (currentQuery.projectId !== undefined) { router.replace({ query: { ...currentQuery, projectId: undefined } }); } } }); const clearProjectFilter = () => { selectedProjectId.value = null; }; // --- view mode (persisted to localStorage) --- const viewMode = ref<ViewMode>(loadViewMode()); watch(viewMode, (newVal) => { localStorage.setItem('task_view_mode', newVal); }); // --- grouping --- const groupBy = ref<GroupBy>('status'); // --- board columns (one set per grouping strategy) --- const statusColumns = computed<TaskBoardColumn[]>(() => [ { id: TaskStatus.TODO, title: t('tasks.todo'), color: 'bg-slate-500', headerBg: 'from-slate-500/10 to-transparent', }, { id: TaskStatus.IN_PROGRESS, title: t('tasks.inProgress'), color: 'bg-accent', headerBg: 'from-accent/10 to-transparent', }, { id: TaskStatus.DONE, title: t('tasks.done'), color: 'bg-emerald-500', headerBg: 'from-emerald-500/10 to-transparent', }, ]); const priorityColumns = computed<TaskBoardColumn[]>(() => [ { id: 'URGENT', title: t('tasks.urgent'), color: 'bg-rose-500', headerBg: 'from-rose-500/10 to-transparent', }, { id: 'HIGH', title: t('tasks.high'), color: 'bg-orange-500', headerBg: 'from-orange-500/10 to-transparent', }, { id: 'MEDIUM', title: t('tasks.medium'), color: 'bg-amber-500', headerBg: 'from-amber-500/10 to-transparent', }, { id: 'LOW', title: t('tasks.low'), color: 'bg-blue-500', headerBg: 'from-blue-500/10 to-transparent', }, { id: 'NONE', title: t('tasks.noPriority'), color: 'bg-slate-400', headerBg: 'from-slate-400/10 to-transparent', }, ]); const assigneeColumns = computed<TaskBoardColumn[]>(() => { const cols: TaskBoardColumn[] = [ { id: 'unassigned', title: '未指派', color: 'bg-slate-400', headerBg: 'from-slate-400/10 to-transparent', }, ]; teamMembers.value.forEach((m) => { cols.push({ id: m.id, title: m.name || '团队成员', color: 'bg-accent', headerBg: 'from-accent/10 to-transparent', }); }); return cols; }); const dueDateColumns = computed<TaskBoardColumn[]>(() => [ { id: 'overdue', title: '已逾期', color: 'bg-rose-500', headerBg: 'from-rose-500/10 to-transparent', }, { id: 'today', title: '今天截止', color: 'bg-orange-500', headerBg: 'from-orange-500/10 to-transparent', }, { id: 'tomorrow', title: '明天截止', color: 'bg-amber-500', headerBg: 'from-amber-500/10 to-transparent', }, { id: 'week', title: '本周截止', color: 'bg-blue-500', headerBg: 'from-blue-500/10 to-transparent', }, { id: 'future', title: '以后截止', color: 'bg-emerald-500', headerBg: 'from-emerald-500/10 to-transparent', }, { id: 'none', title: '无截止日期', color: 'bg-slate-400', headerBg: 'from-slate-400/10 to-transparent', }, ]); const activeColumns = computed<TaskBoardColumn[]>(() => { if (groupBy.value === 'status') return statusColumns.value; if (groupBy.value === 'priority') return priorityColumns.value; if (groupBy.value === 'assignee') return assigneeColumns.value; return dueDateColumns.value; }); const isAnyFilterActive = computed<boolean>(() => { return ( searchQuery.value !== '' || dateFilter.value !== 'all' || statusFilter.value !== 'all' || priorityFilter.value !== 'all' || assigneeFilter.value !== 'all' || tagFilter.value !== 'all' || subtaskDisplay.value !== 'collapse' || hideCompleted.value || onlyMyTasks.value || selectedProjectId.value !== null ); }); const resetAllFilters = () => { searchQuery.value = ''; dateFilter.value = 'all'; statusFilter.value = 'all'; priorityFilter.value = 'all'; assigneeFilter.value = 'all'; tagFilter.value = 'all'; subtaskDisplay.value = 'collapse'; hideCompleted.value = false; onlyMyTasks.value = false; selectedProjectId.value = null; }; const filteredTasks = computed<Task[]>(() => { let filtered = tasks.value; if (searchQuery.value) { const q = searchQuery.value.toLowerCase(); filtered = filtered.filter( (t) => t.title.toLowerCase().includes(q) || t.description?.toLowerCase().includes(q) || (t.parsedTags && t.parsedTags.some((tag: string) => tag.toLowerCase().includes(q))), ); } // Assignee Filter if (assigneeFilter.value !== 'all') { if (assigneeFilter.value === 'unassigned') { filtered = filtered.filter((t) => !t.assigneeId); } else { filtered = filtered.filter((t) => t.assigneeId === assigneeFilter.value); } } // Tag Filter if (tagFilter.value !== 'all') { filtered = filtered.filter((t) => { return t.parsedTags && t.parsedTags.includes(tagFilter.value); }); } if (dateFilter.value !== 'all') { const now = new Date(); now.setHours(0, 0, 0, 0); const endOfDay = new Date(now); endOfDay.setHours(23, 59, 59, 999); if (dateFilter.value === 'overdue') { filtered = filtered.filter( (t) => t.dueDate && new Date(t.dueDate) < now && t.status !== TaskStatus.DONE, ); } else if (dateFilter.value === 'today') { filtered = filtered.filter((t) => { if (!t.dueDate) return false; const d = new Date(t.dueDate); return d>= now && d <= endOfDay; }); } else if (dateFilter.value === 'week') { const endOfWeek = new Date(now); endOfWeek.setDate(now.getDate() + 7); filtered = filtered.filter((t) => { if (!t.dueDate) return false; const d = new Date(t.dueDate); return d>= now && d <= endOfWeek; }); } else if (dateFilter.value === 'custom' && customDate.value) { const selectedDate = new Date(customDate.value); selectedDate.setHours(0, 0, 0, 0); const selectedEnd = new Date(selectedDate); selectedEnd.setHours(23, 59, 59, 999); filtered = filtered.filter((t) => { if (!t.dueDate) return false; const d = new Date(t.dueDate); return d>= selectedDate && d <= selectedEnd; }); } } if (statusFilter.value !== 'all') { filtered = filtered.filter((t) => t.status === statusFilter.value); } if (priorityFilter.value !== 'all') { filtered = filtered.filter((t) => t.priority === priorityFilter.value); } if (hideCompleted.value) { filtered = filtered.filter((t) => t.status !== TaskStatus.DONE); } if (onlyMyTasks.value && authStore.user?.id) { const myId = authStore.user.id; filtered = filtered.filter((t) => t.assigneeId === myId); } if (selectedProjectId.value) { if (selectedProjectId.value === 'unassigned') { filtered = filtered.filter((t) => !t.projectId); } else { filtered = filtered.filter((t) => t.projectId === selectedProjectId.value); } } // Apply sorting if (sortBy.value === 'natural') { filtered = [...filtered].sort((a, b) => { const dayA = a && a.title ? getTaskDayIndex(a.title) : Infinity; const dayB = b && b.title ? getTaskDayIndex(b.title) : Infinity; if (dayA !== dayB) { return dayA - dayB; } return getTaskTime(a) - getTaskTime(b); }); } else if (sortBy.value === 'createdAt_asc') { filtered = [...filtered].sort((a, b) => getTaskTime(a) - getTaskTime(b)); } else if (sortBy.value === 'createdAt_desc') { filtered = [...filtered].sort((a, b) => getTaskTime(b) - getTaskTime(a)); } if (sortOrder.value === 'desc') { filtered = [...filtered].reverse(); } return filtered; }); const allTags = computed<string[]>(() => { const tagsSet = new Set<string>(); tasks.value.forEach((t) => { if (t.parsedTags) { t.parsedTags.forEach((tag) => tagsSet.add(tag)); } }); return Array.from(tagsSet); }); const processedTasks = computed<Task[]>(() => { let list = filteredTasks.value; if (subtaskDisplay.value === 'separate') { const flattened: Task[] = []; list.forEach((t) => { flattened.push(t); if (t.parsedSubtasks && Array.isArray(t.parsedSubtasks)) { t.parsedSubtasks.forEach((sub, index: number) => { flattened.push({ id: `${t.id}_sub_${index}`, title: sub.text, description: `子任务来自: ${t.title}`, status: sub.done ? TaskStatus.DONE : TaskStatus.TODO, priority: t.priority || 'MEDIUM', dueDate: t.dueDate, assigneeId: sub.assigneeId || null, projectId: t.projectId, teamId: t.teamId, tags: t.tags, parsedTags: t.parsedTags, isSubtask: true, parentId: t.id, subtaskIndex: index, project: t.project, assignee: sub.assigneeId ? teamMembers.value.find((m) => m.id === sub.assigneeId) : null, }); }); } }); return flattened; } return list; }); const tasksByProject = computed(() => { const filtered = processedTasks.value; const projectMap: Record<string, { id: string | null; name: string; tasks: Task[] }> = {}; projectMap['unassigned'] = { id: null, name: t('projects.unassignedProject'), tasks: [], }; projects.value.forEach((p) => { projectMap[p.id] = { id: p.id, name: p.title, tasks: [], }; }); filtered.forEach((taskItem) => { const pid = taskItem.projectId || 'unassigned'; if (!projectMap[pid]) { projectMap[pid] = { id: taskItem.projectId || null, name: taskItem.project?.title || t('projects.unknownProject'), tasks: [], }; } projectMap[pid].tasks.push(taskItem); }); const result = Object.values(projectMap).filter((g) => g.tasks.length> 0); result.sort((a, b) => { if (a.id === null) return 1; if (b.id === null) return -1; return a.name.localeCompare(b.name); }); return result; }); const getDueDateGroup = (dateStr: string | null | undefined, status: string): string => { if (!dateStr) return 'none'; const d = new Date(dateStr); const now = new Date(); const dDate = new Date(d.getFullYear(), d.getMonth(), d.getDate()); const nowDate = new Date(now.getFullYear(), now.getMonth(), now.getDate()); const diffTime = dDate.getTime() - nowDate.getTime(); const diffDays = Math.ceil(diffTime / (1000 * 60 * 60 * 24)); if (diffDays < 0) { return status === 'DONE' ? 'none' : 'overdue'; } if (diffDays === 0) return 'today'; if (diffDays === 1) return 'tomorrow'; if (diffDays <= 7) return 'week'; return 'future'; }; const tasksByGroup = computed<Record<string, Task[]>>(() => { const filtered = processedTasks.value; const map: Record<string, Task[]> = {}; if (groupBy.value === 'status') { map[TaskStatus.TODO] = []; map[TaskStatus.IN_PROGRESS] = []; map[TaskStatus.DONE] = []; filtered.forEach((t) => { const status = t.status || TaskStatus.TODO; if (map[status]) map[status].push(t); }); } else if (groupBy.value === 'priority') { map['URGENT'] = []; map['HIGH'] = []; map['MEDIUM'] = []; map['LOW'] = []; map['NONE'] = []; filtered.forEach((t) => { const p = t.priority || 'NONE'; if (map[p]) { map[p].push(t); } else { map['NONE'].push(t); } }); } else if (groupBy.value === 'assignee') { map['unassigned'] = []; teamMembers.value.forEach((m) => { map[m.id] = []; }); filtered.forEach((t) => { if (!t.assigneeId || !map[t.assigneeId]) { map['unassigned'].push(t); } else { map[t.assigneeId].push(t); } }); } else if (groupBy.value === 'dueDate') { map['overdue'] = []; map['today'] = []; map['tomorrow'] = []; map['week'] = []; map['future'] = []; map['none'] = []; filtered.forEach((t) => { const group = getDueDateGroup(t.dueDate, t.status); if (map[group]) map[group].push(t); }); } return map; }); const boardTasksByGroup = computed<Record<string, Task[]>>(() => { return viewMode.value === 'board' ? tasksByGroup.value : {}; }); const listTasksByProject = computed(() => { return viewMode.value === 'list' ? tasksByProject.value : []; }); const calendarTasks = computed<Task[]>(() => { return viewMode.value === 'calendar' ? processedTasks.value : []; }); const completionRate = computed<number>(() => { const total = tasks.value.length; if (total === 0) return 0; return Math.round( (tasks.value.filter((t) => t.status === TaskStatus.DONE).length / total) * 100, ); }); const overdueCount = computed<number>(() => { return tasks.value.filter((t) => isTaskOverdue(t)).length; }); return { // query / filter state searchQuery, dateFilter, statusFilter, priorityFilter, customDate, hideCompleted, onlyMyTasks, sortBy, sortOrder, assigneeFilter, tagFilter, subtaskDisplay, // route / view state selectedProjectId, clearProjectFilter, viewMode, groupBy, // board columns statusColumns, priorityColumns, assigneeColumns, dueDateColumns, activeColumns, // derived lists isAnyFilterActive, resetAllFilters, filteredTasks, allTags, processedTasks, tasksByProject, tasksByGroup, boardTasksByGroup, listTasksByProject, calendarTasks, completionRate, overdueCount, };
}
export type UseTaskBoardQueryReturn = ReturnType<typeof useTaskBoardQuery>;
