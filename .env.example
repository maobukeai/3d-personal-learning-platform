# Frontend Configuration
VITE_API_URL=http://localhost:3001

# P10: Yjs 协同 WebSocket 端点（Fastify 挂载前缀 /ws）
# 留空时自动从 VITE_API_URL 推导（http→ws，并保留同一个 host/port）。
# 生产环境建议显式配置，例如 wss://api.example.com/ws
VITE_FASTIFY_WS_URL=

# Note: Backend environment configuration (DATABASE_URL, JWT_SECRET, SMTP, etc.)
# should be configured in the `server/.env` file. Please refer to `server/.env.example`.
