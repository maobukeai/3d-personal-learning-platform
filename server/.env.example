# Server Port
PORT=3001

# JWT Secret (required, use a long random string in production)
JWT_SECRET=your-secret-key-change-this-in-production

# Database (默认开发使用 SQLite，生产环境请改为 MySQL 连接字符串)
# SQLite 格式 (本地开发): file:./prisma/dev.db
# MySQL 格式 (生产发布): mysql://用户名:密码@主机:端口/数据库名
# 示例: mysql://root:yourpassword@localhost:3306/learning_platform
DATABASE_URL="file:./prisma/dev.db"

# Environment (生产服务器必须设为 production)
NODE_ENV=production

# Frontend URL (for CORS，生产环境改为你的真实域名)
# 例如: FRONTEND_URL=https://yourdomain.com
FRONTEND_URL=http://localhost:5173

# GitHub Token (optional, for higher API rate limits)
GITHUB_TOKEN=

# SMTP Configuration (optional, for email sending)
SMTP_HOST=
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
SMTP_SECURE=true

# Alipay Configuration (optional, for payments)
# ALIPAY_APP_ID=
# ALIPAY_PRIVATE_KEY=
# ALIPAY_PUBLIC_KEY=
# ALIPAY_GATEWAY=https://openapi-sandbox.dl.alipaydev.com/gateway.do